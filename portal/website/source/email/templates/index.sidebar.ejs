<md-list>
        <md-list-item ng-repeat="item in email.templates" ng-click="email.loadTemplate(item)" ng-class="{active:item==template.name}">
            {{item}}
        </md-list-item>
</md-list>
