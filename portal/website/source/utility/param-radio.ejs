<md-radio-group ng-if="parameter.type=='radio'" ng-model="runOptions[parameter.name]" layout="row">
    <md-radio-button ng-value="value.name" class="md-primary" ng-repeat="value in parameter.values">
      {{value.title}}
    </md-radio-button>
</md-radio-group>
