<md-list>
    <%
    var docs = site.pages.find({
      template: 'documentation'
    });
    docs.forEach(function(doc) {%>
        <md-list-item ng-href="<%-get_path(doc)%>" ng-class="{active:<%-isActive(doc)%>}">
            <%-lng(doc.title)%>
        </md-list-item>
        <%})%>
</md-list>
