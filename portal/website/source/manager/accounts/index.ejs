<%
var roleTabs = get_data('account/menu') || {};
roleTabs = _.values(roleTabs);
%>

<%-template('manager/accounts/toolbar')%>

<div ng-if="viewAccount.deactivated" class="action-warn" layout="row" layout-align="start center">
  <span>Account is deactivated</span>
  <span flex="auto"></span>
  <md-button class="md-raised" ng-click="edit.activateAccount()">Activate</md-button>
</div>

<div class="account-view frame ag-margin md-padding" ng-show="viewAccount" layout="column">
    <%-template('manager/accounts/overview')%>
    <%-template('manager/accounts/expander',{title:'Role',name:'role',list:roleTabs})%>
    <%-template('manager/accounts/expander',{title:'Settings',name:'tool',list:page.sections})%>
</div>

<%-template('manager/accounts/empty')%>
<%-template('manager/accounts/toast')%>
<%-template('manager/accounts/check')%>
