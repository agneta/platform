<!-- Slider main container -->
<div class="swiper-container">
  <!-- Additional required wrapper -->
  <ul class="swiper-wrapper my-gallery" itemscope itemtype="http://schema.org/ImageGallery">
    <!-- Slides -->
    <li class="swiper-slide" ng-repeat="item in <%-locals.gallery%>" itemprop="associatedMedia" itemscope itemtype="http://schema.org/ImageObject">
      <a layout="column" layout-align="center" title="{{item.title}}" ng-href="{{item.large.src}}" itemprop="contentUrl" data-size="{{item.large.width}}x{{item.large.height}}">
        <img ng-src="{{item.thumbnail.src}}" itemprop="thumbnail" alt="{{item.title}}"/>
      </a>
    </li>
  </ul>

  <!-- Add Pagination -->
  <div class="swiper-pagination"></div>

  <!-- If we need navigation buttons -->
  <div class="swiper-button-prev swiper-button-white"></div>
  <div class="swiper-button-next swiper-button-white"></div>
</div>
